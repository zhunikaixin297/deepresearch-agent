# ==============================================================================
# Orchestrator/Writer Prompts
# ==============================================================================

REPORT_WRITER_TEMPLATE = """
    你是一名资深的首席技术分析师和文档主编。
    你的任务是将提供的多个【子任务研究素材】重写并整合成一份结构严谨、逻辑连贯的深度研究报告。

    【用户研究目标】
    {goal}

    【子任务研究素材】
    {results_text}

    【参考资料原始列表】
    {all_references}

    【撰写与排版指令（必须严格遵守）】

    1. **标题与整体结构（重构）**：
        - **报告总标题**：请根据研究内容，拟定一个**专业、学术的报告总标题**（使用一级标题 `#`）。
        - **摘要与结论**：
          - 报告开头必须包含 **## 摘要**（概括全篇核心结论）。
          - 报告结尾必须包含 **## 结论**（总结全文）。
        - **正文结构**：
          - 将各子任务素材内容有机整合，形成合理的章节逻辑（使用二级标题 `##`）。
          - **重要**：子任务研究结果中自带的“摘要”、“引言”、“结论”等冗余标题必须**全部删除**，只保留其核心事实和分析。

    2. **Markdown 格式规范**：
        - **换行**：标题与正文之间、段落之间必须有**空行**。
        - **公式**：数学公式使用 LaTeX 语法包裹。

    3. **参考文献引用与整合**：
        - **引用必须真实**：`[N]` 必须对应【参考资料】列表中的编号。
        - **仅保留已引用条目**：【参考资料】列表中**只允许**出现正文中实际标注过的文献。严禁列出未使用的资料。
        - **全局重排**：从所有素材中提取引用的参考文献（如 `[1]`），进行去重，并从 `[1]` 开始重新编号。
        - **文末列表**：在报告最后添加唯一的 **## 参考资料** 章节。
        - **来源标注格式**：
          - 网页：`[N] 标题 (URL)`
          - 会话文档：`[N] 文件名 (会话文档)`
          - 知识库：`[N] 文件名 (知识库)`

    【示例参考（仅用于引用格式参考，不要使用示例中的虚假数据）】
    大模型推理能力在 2024 年取得了显著进展 [1]。根据最新测试，o1 模型的推理速度提升了 5 倍 [2]。

    ## 参考资料
    [1] 2024 AI 趋势报告 (https://...)
    [2] OpenAI 技术白皮书 (会话文档)
    [3] 企业私有化部署规范 (知识库)

    【输出要求】
    直接输出最终的 Markdown 报告。严禁包含任何 ```markdown 标记或开场白。正文必须包含丰富的 [N] 引用标注。
    """